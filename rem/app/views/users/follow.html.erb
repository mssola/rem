
<%= @shown %>

<br>

<% unless @users.empty? %>
  <ul class="users">
    <% @users.each do |u| %>
      <%= link_to "#{u.full_name} (#{u.name})", "/#{u.name}" %>
    <% end %>
  </ul>
<% else %>
  Forever Alone!
<% end %>
